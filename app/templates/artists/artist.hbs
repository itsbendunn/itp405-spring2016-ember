{{#if model.songs}}
<h4>
  {{model.artist.artistName}} Songs
</h4>

  {{#each model.songs as |song|}}
    <p>
      Title: {{song.title}}
    </p>
    <p>
      Price: ${{song.price}}
    </p>
    <hr/>
  {{/each}}
{{else}}
<h4>No Songs For Artist</h4>
{{/if}}



{{outlet}}
